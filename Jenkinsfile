//Jenkinsfile (Declarative Pipeline)
/* Requires the Docker Pipeline plugin */
pipeline {
    agent { label { 'jenkins_agent' } }
    stages {
        stage('build') {
            steps {
                echo 'hello world'
            }
        }
    }
}
